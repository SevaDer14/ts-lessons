// Boolean Variable:
// Declare a variable called isBoolean and assign it a true value
export function declareBoolean(): boolean {

  return myBoolean;
}

// Logical NOT:
// Declare a variable called isNotMyBool that contains result of logical negation of myBool
export function logicalNot(myBool: boolean): boolean {

  return isNotMyBool;
}

// Logical OR:
// Declare a variable called condition that is a result of logical disjunction of isAdmin and isUser
export function logicalOr(isAdmin: boolean, isUser: boolean): boolean {

  return condition;
}

// Is truthy:
// Declare a variable called isTruthy that check is input is truthy
export function checkIsTruthy(input: any): boolean {

  return isTruthy;
}

// Compare numbers:
// Declare a variable called areEqual that checks number1 and number2 are equal
export function compareNumbers(number1: number, number2: number): boolean {

  return areEqual;
}

// Compare string lengths:
// Declare a variable called isLonger that checks if string1 is longer than string2
export function compareStringLengths(
  string1: string,
  string2: string
): boolean {
  
  return areEqual;
}

export function declareNumber() {
    return 5
}

// Basic Variable Declaration:
// Declare a variable called myNumber and assign it a numerical value. Log the variable to the console.

// String Variable:
// Declare a variable called myString and assign it a string value. Log the variable to the console.

// Boolean Variable:
// Declare a variable called isTrue and assign it a boolean value. Log the variable to the console.

// Type Inference:
// Declare a variable without explicitly specifying its type. Assign it a value of your choice. Log the variable to the console.

// Explicit Typing:
// Declare a variable with explicit typing. For example, declare a variable myExplicitNumber with the type number. Assign it a numerical value and log it to the console.

// Union Types:
// Declare a variable with a union type. For instance, a variable myUnion that can hold either a string or a number. Assign values of both types and log the variable.

// Any Type:
// Declare a variable with the any type. Assign values of different types and log the variable. Discuss the flexibility and drawbacks of using any.

// Array Declaration:
// Declare an array variable called myArray and assign it an array of numbers. Log the array to the console.

// Tuple Declaration:
// Declare a tuple variable called myTuple with a specified structure, e.g., [string, number]. Assign values accordingly and log the tuple.

// Object Declaration:
// Declare an object variable called myObject with key-value pairs representing a person's information (name, age, etc.). Log the object to the console.

// Constant Variable:
// Declare a constant variable called PI and assign it the value of π (approximately 3.14159). Attempt to reassign a new value and discuss the error.

// Destructuring Assignment:
// Declare an object with properties like { firstName: string, lastName: string }. Use destructuring assignment to create variables first and last that hold the corresponding values.

// Variable Scope:
// Declare a variable inside a function and another one outside the function. Attempt to access both variables outside the function and discuss variable scope.

// Template Strings:
// Declare variables for a person's name and age. Use a template string to create a sentence like "John is 25 years old" and log it to the console.

// Type Aliases:
// Create a type alias for a complex object structure, such as a user profile with multiple properties. Declare a variable with this type and log it to the console.